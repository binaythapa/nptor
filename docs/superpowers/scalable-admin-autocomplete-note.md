# Scalable Admin Access Selectors

The Access & Subscription Control Panel uses server-side prefix autocomplete for users, courses, and tracks instead of rendering every record in HTML. Search results are limited to 20 records and requests are debounced in the browser. Course plans are returned only for the selected course. Individual exams remain unavailable as sellable products.
