# OpenAI configuration for the CV AI Writer & Reviewer
# Keep the real API key out of Git and set it in the server environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
OPENAI_BASE_URL=https://api.openai.com/v1
