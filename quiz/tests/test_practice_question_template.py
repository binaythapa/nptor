from pathlib import Path


# Existing tests in this module cover the Express question template and shared
# UI integration. The contract below intentionally checks the classes the
# Express adapter actually applies at runtime.

# ...
